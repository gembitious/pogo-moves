---
import Base from '@/layouts/Base.astro'
import MovePage from '@/components/MovePage.tsx'
import { fastMoves, chargedMoves } from '@/lib/moves'
import { getDictionary, type Locale } from '@/lib/i18n'

export function getStaticPaths() {
  return [{ params: { lang: 'ko' } }, { params: { lang: 'en' } }]
}

const lang = Astro.params.lang as Locale
const dict = getDictionary(lang)
---

<Base locale={lang} title={dict.nav.move} description={dict.meta.desc.move}>
  <MovePage client:load locale={lang} dict={dict} fast={fastMoves} charged={chargedMoves} />
</Base>
